version: '3'

services:

  backend:
    build: ./backend

    ports:
      - "8000:8000"

    depends_on:
      - redis
      - mongodb
      - postgres

  worker:
    build: ./backend

    command: python -m app.worker

    depends_on:
      - redis
      - mongodb
      - postgres

  frontend:
    build: ./frontend

    ports:
      - "3000:3000"

    depends_on:
      - backend

  redis:
    image: redis

    ports:
      - "6379:6379"

  mongodb:
    image: mongo

    ports:
      - "27017:27017"

  postgres:
    image: postgres

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ims

    ports:
      - "5432:5432"
